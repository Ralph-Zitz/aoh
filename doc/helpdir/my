COMMAND
	my =[<channelname>] <text>

DESCRIPTION
	Send a text on channel <channelname> in possessive case (genitive).
	If the channelname is not given, the players channel is used.

EXAMPLES
	my = lag is gone  
	  -> produces [players:<yourname>'s lag is gone]
	my =#sports lag is still here
	  -> produces [#sports:<yourname>'s lag is still here]

SEE ALSO

	channels, =, ==, to
