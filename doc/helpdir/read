SYNOPSIS
    read <object>
    read <msg> on <object>

DESCRIPTION
    Read a message from the specified <object>, if there is any.
    The object may be carried or lying around. If <object> would match
    several real objects, just the first found is selected and read.

    It is possible to read within bags and/or several objects at once.
    Example: 'read paper in box', 'read the book and the scroll'.

SEE ALSO
    examine, look
