SYNOPSIS
	void pshout(string str)

DESCRIPTION
	Send a string str to all non-wizard users. Messages send with
	pshout() won't be catched by npcs.

SEE ALSO
	write(E), tell_object(E), say(E), shout(E)
