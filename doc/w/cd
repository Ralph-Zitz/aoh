USAGE
  cd [<newpath>]
  cd @

  Change the current directory, either to <newpath> (default is the
  home directory), or the the directory containing the room you are
  currently in.
  If the target directory contains a file '.readme', it is displayed.
  The file should be used to hold a short (max two lines) description
  of the directory's content.
