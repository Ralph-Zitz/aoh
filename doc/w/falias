falias [<alias> [<exp>]]

Without arg: all defined file-aliases are displayed.

With one arg: the file-alias <alias> is removed.

With two args: the file-alias <alias> is set to <exp>. Before <exp> itself
is expanded using all other defined file-aliases.

See also: w/filealiases
