Command: status
Argument: 'tables' or none

When given w/o argument, some memory usage statistics
are printed, giving the count of allocated memory objects
and the amount of memory used.
When given w/ argument 'tables', some statistics about
the internal hash tables are printed.

This command is hardcoded into the gamedriver.

See also: w/malloc, w/dumpallobj
