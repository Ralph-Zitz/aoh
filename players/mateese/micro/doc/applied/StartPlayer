SYNOPSIS
  void StartPlayer (string name, int guestno)

IMPLEMENTED
  Player object.
  Called by /secure/login.

DESCRIPTION
  Informs the player object that was connected to an interactive user
  and thus may start operations.
    name   : name of the player (in the right case)
    guestno: for guest players, the ordinal number.

