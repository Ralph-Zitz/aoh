SYNOPSIS
  varargs int clonep (void | object obj)

DESCRIPTION
  Return non-zero if <obj> (default is the current object) is
  a clone.
  Interactive objects always count as clones.

SEE ALSO
  blueprint(sefun)
