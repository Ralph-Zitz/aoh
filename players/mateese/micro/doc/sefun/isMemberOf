SYNOPSIS
  int isMemberOf (string domain, string|object ob)  [MACRO]

DESCRIPTION
  Return non-zero if <ob> is member of <domain>.

SEE ALSO
  isPlayer(sefun), isLearner(sefun), isWizard(sefun), isAdmin(sefun),
  isArch(sefun), isLordOf(sefun), domainlevel(sefun), wizlevel(sefun)

