SYNOPSIS
  int isPlayer (string|object ob)  [MACRO]

DESCRIPTION
  Return non-zero if <ob> has player rights.

SEE ALSO
  isLearner(sefun), isWizard(sefun), isAdmin(sefun), isArch(sefun),
  isMemberOf(sefun), isLordOf(sefun), domainlevel(sefun), wizlevel(sefun)
