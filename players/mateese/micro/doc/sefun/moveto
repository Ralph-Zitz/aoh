SYNOPSIS
  void moveto (string|object dest)  [MACRO]

DESCRIPTION
  this_object() is moved into <dest>.
  However, direct use is discouraged, use ->move() instead since the
  lfun keeps track of the weights and lights.

SEE ALSO
  remove(A), init(A), native(C), move_object(E), moving(C)
