SYNOPSIS
  void shutdown()

DESCRIPTION
  Shutdown the mud. Never use this efun. Instead if you have a
  need to shutdown the mud use the 'shutdown' command.
  A log entry in /log/adm/game is made.

SEE ALSO
  break_point(E), swap(E)
