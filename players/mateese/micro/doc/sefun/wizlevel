SYNOPSIS
  int wizlevel (string|object ob)  [MACRO]

DESCRIPTION
  Returns the wizlevel for <obj>. It is an integer out of
  PLAYER_LVL, LEARNER_LVL, WIZARD_LVL, ARCH_LVL and ADMIN_LVL.

SEE ALSO
  isAdmin(sefun), isArch(sefun), isLearner(sefun), isPlayer(sefun),
  isWizard(sefun), isMemberOf(sefun), isLordOf(sefun),
  domainlevel(sefun)
