This directory contains system include files that define constants etc.

!!! include these files into your objects and use the symbolic defined
!!! names, instead of hardcoded values

driver_hook.h functionlist.h erq.h lpctypes.h sent.h wizlist.h
do not depend on the mudlib or the installation, but on the
driver version. They are readonly. 
